# Facilities AI Assistant Web

Next.js frontend for the M1 incident flow and M2 assistant trace.

```bash
pnpm install
pnpm dev
```

The application uses `http://localhost:8000/api/v1` by default. Override it with
`NEXT_PUBLIC_API_URL` when the API is hosted elsewhere.
